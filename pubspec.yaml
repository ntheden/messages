name: messages
description: Messaging app over nostr protocol
publish_to: "none"

flutter:
  assets:
    - assets/nostr_logo_prpl.svg

  uses-material-design: true

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter
  after_layout: ^1.0.7+2
  path_provider: ^2.0.14
  sqlite3_flutter_libs: ^0.5.13
  prompt_dialog: ^1.0.9
  nostr:
    git:
      url: https://github.com/no-prob/dart-nostr.git
      ref: message-types
      resolved-ref: "3b1bf377f73e1dd4c80b02dde9a9ef51fa9003df"
  file_picker:
    git:
      url: https://github.com/miguelpruivo/flutter_file_picker.git
  drift: ^2.7.0
  badges: ^3.1.1
  intl: ^0.18.0
  timezone: ^0.9.2
  flutter_contacts: ^1.1.6
  rxdart: ^0.27.7
  get: ^4.6.5
  shared_preferences: ^2.1.1
  multiavatar: ^0.1.5
  flutter_svg_icons: ^0.0.1+3


dev_dependencies:
  build_runner: ^2.3.3
  drift_dev: ^2.7.0
  flutter_lints: ^2.0.1
  test: ^1.24.2
